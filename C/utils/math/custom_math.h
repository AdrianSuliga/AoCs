#ifndef CUSTOM_MATH_H
#define CUSTOM_MATH_H

#define TRUE 1
#define FALSE 0

#define MAX(a, b) ((a) >= (b) ? (a) : (b)) 
#define MIN(a, b) ((a) <= (b) ? (a) : (b))

enum cmd {
    RUN,
    TEST
};

#endif /* CUSTOM_MATH_H */
